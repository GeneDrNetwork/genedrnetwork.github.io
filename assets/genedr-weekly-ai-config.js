// Static GitHub Pages must never contain an AI provider secret.
// Set endpoint to a separately hosted, authenticated server-side proxy if one is available.
window.GENEDR_WEEKLY_AI_CONFIG = {
  endpoint: "",
  timeoutMs: 45000
};
